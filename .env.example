# Copy to .env (git-ignored). Next.js loads it into the dev server, where the /api/explain route
# reads the key to write explanations on demand; the key never reaches the browser.
ANTHROPIC_API_KEY=
